name: EfficientPhys 200 FPS Stage3 Pixel Engines

on:
  push:
    branches: [effphys-200fps-rtl-verify-20260820]
    paths:
      - 'effphys_200fps_rtl/**'
      - '.github/workflows/effphys-200fps-stage3.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  simulate-synthesize:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: effphys-200fps-rtl-verify-20260820
      - run: |
          sudo apt-get update
          sudo apt-get install -y iverilog verilator yosys
      - id: regression
        shell: bash
        run: |
          set +e
          bash effphys_200fps_rtl/run_stage3.sh
          rc=$?
          echo "rc=$rc" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Record result
        if: always()
        shell: bash
        run: |
          cd effphys_200fps_rtl
          rm -f STAGE3_PASS.txt STAGE3_FAIL.txt
          rc='${{ steps.regression.outputs.rc }}'
          result=STAGE3_FAIL.txt
          [ "$rc" = "0" ] && result=STAGE3_PASS.txt
          {
            echo "RUN_RC=$rc"
            echo "COMMIT=$GITHUB_SHA"
            echo "--- REGRESSION LOG ---"
            cat build3/STAGE3_RESULT.txt 2>/dev/null || true
          } > "$result"
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add -A
          git commit -m '[skip ci] test: record EfficientPhys Stage3 RTL result' || true
          git push origin HEAD:effphys-200fps-rtl-verify-20260820
